<div class="item-list-block">
	<div class="item-list-header">
		<h3> {{#if resultsLabel}} {{resultsLabel}} {{else}} Results {{/if}} </h3>
		{{#if pagingInfo}} 
			{{paging-component pagingInfo=pagingInfo 
				nextPage=pagingInfo.nextPage 
				prevPage=pagingInfo.prevPage
			}}
		{{/if}}	
	</div>

	<div class="item-list">	
		{{#each items as |item|}}

			{{#if hasBlock}}	
				HAS BLOCK {{item.title}} 	
			  	{{yield item}}
			{{else}}
			    <section class="item">
			      <span class="title">
			      	{{#if item.link}} 
			      		{{#link-to item.link item.id}} 
			      			{{item.title}} 
			      		{{/link-to}}
			      	{{else}} 
			      		{{item.title}}
			      	{{/if}}
			      </span>
			      <p>{{item.description}}</p>
			    </section>
			{{/if}}
		{{/each}}	
	</div>
</div>