import Ember from 'ember';
import whereComponentModel from 'dxref/components/where/where-model';
export default Ember.Component.extend({
	classNames: ['where-container'],
	whereInfo : whereComponentModel	
});
