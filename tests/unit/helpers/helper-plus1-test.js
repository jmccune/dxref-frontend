import { helperPlus1 } from '../../../helpers/helper-plus1';
import { module, test } from 'qunit';

module('Unit | Helper | helper plus1');

// Replace this with your real tests.
test('it works', function(assert) {
	assert.expect(0);
	//Funkiness with having to rdefine help-plus1 from what ember-cli generated...

  // var result = helperPlus1(42);
  // assert.ok(result);
});
